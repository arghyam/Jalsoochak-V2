package org.arghyam.jalsoochak.anomaly.config;

import org.springframework.context.annotation.Configuration;

/**
 * DataSource configuration placeholder.
 * <p>
 * Spring Boot auto-configures the DataSource from application.yml properties.
 * Add custom DataSource beans here when multi-tenancy or connection-pool
 * tuning is required.
 */
@Configuration
public class DataSourceConfig {
    // Auto-configured via application.yml
}
