server:
  port: 8084

spring:
  application:
    name: telemetry-service

  datasource:
    url: jdbc:postgresql://localhost:5432/jalsoochak_db?stringtype=unspecified
    username: jalsoochak_user
    password: Password7@

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: telemetry-service-group
      auto-offset-reset: earliest

minio:
  endpoint: http://192.168.6.61:9000
  bucket: jalsoochak-dev
  access-key: D0NW7W59XKFHEA866VG1
  secret-key: q6Mku87XebrNW2zXfoQTypz8QChl3CF1CQZtPkcx
  region: us-east-1

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"